### my-project
## Структура проекта на GitHub:
```
my-project/                                  # Главная папка проекта
│
├── api_example/                             # Пример работы с API и сохранения данных
│   ├── api_reader.py                        # Скрипт для обращения к API (например, Open Food Facts)
│   ├── screenshot.png                       # Результат запуска скрипта
│   └── README.md                            # Описание логики работы с API и шагов получения данных
│
├── notebooks/                               # Папка с Jupyter ноутбуками для анализа данных
│   └── EDA.ipynb                            # Основной ноутбук для разведочного анализа данных (EDA)
│   └── README.md                            # Описание логики работы с EDA и шагов получения данных
│
├── parse_example/                           # Пример парсинга данных из веб-источников
│   ├── data_parser.py                       # Скрипт для парсинга (сбор данных из сайтов)
│   ├── screenshot.png                       # Результат запуска скрипта
│   └── README.md                            # Пояснение по парсеру и особенности кода
│
│
├── README.md                                # Описание логики работы с проектом и шагов получения данных
├── data_loader.py                           # Скрипт для загрузки и первичной обработки данных
├── requirements.txt                         # Список библиотек и зависимостей для установки (pip install -r requirements.txt)
└── write_to_db.py                           # Скрипт для записи данных из датасета в БД PostgreSQL
```

⚙️ Файлы `venv` и `creds.db` находятся локально и не загружаются в репозиторий (виртуальная среда и конфиденциальные данные).

⚙️ Файлы `clean_data.parquet` (очищенный и подготовленный датасет для анализа), `quotes.parquet` (сохранённые результаты парсинга), `open_food_facts.parquet` (сохранённые данные после загрузки через API) также находятся локально и не загружались на GitHub.

## 📊 Просмотр EDA ноутбука

Посмотреть интерактивную версию ноутбука можно по ссылке:

👉 [Открыть EDA.ipynb через nbviewer.org](https://nbviewer.org/github/dark1ing0/my-project/blob/main/notebooks/EDA.ipynb)

---

🌿 Ссылка на датасет Bio Ethanol Production Dataset: https://drive.google.com/file/d/1odXw81javPy4RwmYhj4IZts80g-3rMOv/view?usp=sharing

---

## 1️⃣ Создание виртуального окружения
В папке с проектом создала виртуальное окружение:
powershell
`python -m venv venv`
Активировала окружение в PowerShell:
`.\venv\Scripts\Activate.ps1`
Установила необходимые библиотеки:
`pip install pandas`
Когда библиотеки были установлены, я зафиксировала их в файл requirements.txt. Это позволяет в будущем быстро установить такие же пакеты, если проект переносится на другой компьютер:
`pip freeze > requirements.txt`
Если другой человек захочет повторить мою работу, ему достаточно активировать окружение и установить все зависимости командой:
`pip install -r requirements.txt`

## 2️⃣ Загрузка датасета
Создала файл data_loader.py и вставила туда код:
`import pandas as pd`

`FILE_ID = "1odXw81javPy4RwmYhj4IZts80g-3rMOv"` # Google Drive file id

`file_url = f"https://drive.google.com/uc?id={FILE_ID}"`

`raw_data = pd.read_csv(file_url)` # Читаем CSV

Скрипт `data_loader.py` читает датасет и выводит первые 10 строк.
Результат работы скрипта:
<img width="1919" height="1079" alt="image" src="https://github.com/user-attachments/assets/821898de-e93b-49c8-86bd-a0b1efc4c3b7" />

## 3️⃣ Приведение типов колонок
В моем датасет 3 вида типа данных:
Тип данных | Примеры | Применение
--- | --- | ---
int | 2023, 5, -12 | Целые числа: сортировка, арифметика, подсчёт сумм
float | 3.14, 0.5, -12.7 | Дробные числа: усреднение, вычисления, графики
str | "USA", "Hello", "123" | Текст: фильтрация, группировка, поиск, замена

`print("\nТипы колонок после приведения:")`

`print(raw_data.dtypes)`

Пример приведения типов:
`raw_data["Year"] = raw_data["Year"].astype(int)`

Приведение типов должно выполняться после того, как данные уже загружены в raw_data. Если писать до `raw_data = pd.read_csv(...)`, переменной ещё нет, и Python выдаст ошибку. Получается: Загружаем данные из CSV (`raw_data = pd.read_csv(file_url)`) → Приводим колонки к нужным типам → Выводим проверку (`print(raw_data.head(10))`)

Результат работы скрипта:
<img width="1919" height="1074" alt="hw3" src="https://github.com/user-attachments/assets/5728ade9-6ee6-4ca0-9731-5ff6cfb57742" />

Далее создание clean_data.parquet: `raw_data.to_parquet("clean_data.parquet", index=False)` 

Далее надо установить дополнительную библиотеку: `pip install pyarrow`

После установки и запуска скрипта появился файл. В нём лежат все строки DataFrame с правильными типами.

## 4️⃣ Подключение к базе данных PostgreSQL с использованием учетных данных из локальной базы и загрузка в неё части своего датасета в виде новой таблицы
Сначала нужно установить библиотеки.

Для работы понадобились:
* `pandas` - для работы с датасетом;
* `sqlite3` - для чтения данных из локальной БД `creds.db`;
* `sqlalchemy` и `psycopg2` - для подключения к PostgreSQL.

1.  Чтение учетных данных из creds.db
- В файле `creds.db` лежат данные для входа в базу.
- Создала файл `write_to_db.py` и в нем подключилась к базе SQLite, чтобы получить данные для подключения
- Подключалась через `sqlite3` и считала хост, порт, логин и пароль.

⚠️ Эти данные нигде не выкладываются и не вставляются в код вручную - они подгружаются автоматически.

2. Подготовка датасета

Датасет у меня был на Google Диске. Я скачала его в программу и оставила только первые 100 строк. Это та часть данных, которые были залиты в таблицу.

3. Подключение к базе PostgreSQL
- С помощью `sqlalchemy` я подключилась к базе, используя данные из `creds.db`.
- Название своей таблицы я задала как `revyakina`.

4. Загрузка данных в таблицу

С помощью команды ниже я загрузила подготовленные данные в базу:

`df.to_sql(table_name, con=engine, schema="public", if_exists="replace", index=False)`

После успешного выполнения в терминале появилось соответствующее сообщение.

⚠️ Важно помнить
* `creds.db` нельзя отправлять или загружать в GitHub.
* Если перезапустить код - таблица просто перезапишется.
